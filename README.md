# flux_loader

`flux_loader` 是一个基于 ESP32 的步进电机控制与物料传输系统。该系统通过精确控制 8 个步进电机，驱动 8 个一体化托架转轮，实现物料（芦笋/Asparagus）的节拍式顺序传输与分发。

---

## 1. 硬件架构与控制

- **核心控制器**：ESP32
- **执行机构**：8 个步进电机（分别控制 8 个托架转轮）
- **机械结构**：
  - 系统包含 8 个托架转轮（托架与转轮一体化设计）。
  - **托架转轮编号**：从右向左依次为 **1号托架转轮** 至 **8号托架转轮**。
  - **传输方向**：物料（芦笋）由左向右依次传输，即：
    $$\text{8号托架转轮} \rightarrow \text{7号} \rightarrow \text{6号} \rightarrow \text{5号} \rightarrow \text{4号} \rightarrow \text{3号} \rightarrow \text{2号} \rightarrow \text{1号}$$

---

## 2. 物料状态定义

对于任意托架转轮上的芦笋数量，系统主要关心以下三种状态：
1. **0 个芦笋**：表示托架为空。
2. **1 个芦笋**：表示托架上仅有单个芦笋。
3. **2 个及以上芦笋（2+）**：多物料状态（2 个与 2 个以上在业务处理逻辑中视为相同状态）。

---

## 3. 业务控制逻辑（节拍式运行）

系统按**节拍（周期）**运行。在每一个工作周期开始时，各个托架转轮的动作由其自身及相邻右侧托架转轮的物料状态决定。

### 核心规则

1. **1号托架转轮（最右侧出口）**：
   - 处于固定工作状态，每个节拍**固定旋转 90°**。

2. **2号至8号托架转轮（$N$ 号托架转轮，其中 $N \ge 2$）**：
   - 是否转动及转动角度，取决于其**右侧相邻托架转轮（$N-1$ 号托架转轮）**的物料状态，以及**自身**的物料状态。

| 右侧 $N-1$ 号托架转轮状态 | 自身 $N$ 号托架转轮状态 | $N$ 号托架转轮动作 | 说明 |
| :--- | :--- | :--- | :--- |
| **有芦笋** ($\ge 1$) | 任意 | **停止** (旋转 0°) | 避免向已有芦笋的右侧托架转轮继续输送物料，防止堆积或干涉 |
| **无芦笋** ($== 0$) | **无芦笋** ($== 0$) | **旋转 90°** | 右侧为空且自身为空时，正常旋转以接收左侧（上游）传来的物料 |
| **无芦笋** ($== 0$) | **有 1 个芦笋** | **旋转 90°** | 将单物料向前输送 |
| **无芦笋** ($== 0$) | **有多个芦笋** ($\ge 2$) | **旋转 22.5°** | 多物料状态下，通过减小旋转角度降低传输速度，将多根芦笋逐步分解为单根后再向右侧输送 |

---

## 4. 蓝牙通信与数据流

系统中的物料数据通过无线蓝牙通信与移动端（手机）进行交互。

1. **连接建立**：
   - ESP32 启动后需建立与手机的蓝牙连接。
2. **下行数据（手机 $\rightarrow$ ESP32）**：
   - 在每个节拍开始前，手机通过蓝牙向 ESP32 发送当前 1 至 8 号托架转轮上的**芦笋数量**。
   - ESP32 接收并解析该数据，作为当前节拍控制逻辑的判定输入。
   - **超时行为**：若 ESP32 未收到手机数据，则持续等待，不执行任何电机动作，直至数据到达为止。
3. **上行数据（ESP32 $\rightarrow$ 手机）**：
   - 当一个节拍动作执行完毕（各电机完成旋转/停止动作）后，ESP32 通过蓝牙向手机发送**节拍结束信号**。
   - 告知手机当前周期已完成，请求获取下一个周期的托架芦笋数量数据。

---

## 5. 多电机同步执行策略

由于 8 个步进电机共用同一路 STEP 脉冲信号（`SHARED_STEP_PIN`），通过各自的 EN 引脚独立选通，在同一节拍内不同电机可能需要执行不同角度的旋转（90° 或 22.5°）。系统采用**两阶段脉冲策略**：

1. **第一阶段（22.5° 脉冲）**：对所有需要动作的电机（包括需要旋转 90° 和 22.5° 的电机）同时开放 EN 选通，发出对应 22.5° 所需的脉冲数。
2. **第二阶段（补充脉冲）**：仅对需要旋转 90° 的电机保持 EN 选通，继续发出补充脉冲（共 90° - 22.5° = 67.5° 所需的脉冲数），使其达到 90° 总旋转量。需要旋转 22.5° 的电机此时 EN 已关闭，不再接收脉冲。

> **细分设置**：所有驱动器共用同一组细分控制引脚（MS1 / MS2 / MS3），统一配置为相同细分模式，整个节拍过程中细分设置保持不变。

---

## 6. 接口与引脚定义 (GPIO)

*(接口电路定义与 GPIO 引脚分配将在后续开发阶段补充和完善。)*

