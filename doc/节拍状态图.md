# 节拍状态图

> 全新定义的流程文档。一次完整的状态流转按九个模块划分，每个模块一张独立状态图。
> 模块编号从90开始（90~98），卡片编号从1开始（1~30），画线时可直接用「编号到编号」描述。
> 节拍模块内的元素统称「旋转」。

## 总图

```mermaid
flowchart TD
    classDef st fill:#000000,stroke:#ffffff,stroke-width:2px,color:#ffffff
    classDef str fill:#8b0000,stroke:#ffffff,stroke-width:2px,color:#ffffff
    classDef str fill:#8b0000,stroke:#ffffff,stroke-width:2px,color:#ffffff
    classDef stg fill:#1b5e20,stroke:#ffffff,stroke-width:2px,color:#ffffff

    style M1 fill:#222222,stroke:#ffffff,stroke-width:1px,stroke-dasharray:5 5,color:#ffffff
    style M2 fill:#222222,stroke:#ffffff,stroke-width:1px,stroke-dasharray:5 5,color:#ffffff
    style M3 fill:#222222,stroke:#ffffff,stroke-width:1px,stroke-dasharray:5 5,color:#ffffff
    style M4 fill:#222222,stroke:#ffffff,stroke-width:1px,stroke-dasharray:5 5,color:#ffffff
    style M5 fill:#222222,stroke:#ffffff,stroke-width:1px,stroke-dasharray:5 5,color:#ffffff
    style M6 fill:#222222,stroke:#ffffff,stroke-width:1px,stroke-dasharray:5 5,color:#ffffff
    style M7 fill:#222222,stroke:#ffffff,stroke-width:1px,stroke-dasharray:5 5,color:#ffffff
    style M8 fill:#222222,stroke:#ffffff,stroke-width:1px,stroke-dasharray:5 5,color:#ffffff
    style M9 fill:#222222,stroke:#ffffff,stroke-width:1px,stroke-dasharray:5 5,color:#ffffff

    subgraph M1["模块90 上电初始化"]
        PWR([1 上电]):::st --> FREE1("2 自由"):::st
    end

    subgraph M2["模块91 整备A"]
        LKA("3 锁定"):::st
        SA("4 转世"):::str
    end

    subgraph M3["模块92 美妙配对"]
        WD("5 美妙"):::st
        GK("6 接美妙"):::st
    end

    subgraph M4["模块93 超载配对"]
        OV("7 超载"):::st
        JOV("8 接超载"):::st
    end

    subgraph M5["模块94 新生配对"]
        ZS("9 新生"):::str
        JS("10 接新生"):::st
    end

    subgraph M6["模块95 节拍1"]
        ROT60("11 旋转60°"):::st
        ROT30("12 旋转30°"):::st
        ROT28B("13 旋转28°"):::st
        RWAIT("23 空拍"):::st
        DELAY1("25 旋转30°"):::st
        ROT32B("26 旋转32°"):::st
    end

    subgraph M7["模块96 节拍2"]
        ROT60B("14 旋转60°"):::st
        ROT90("15 旋转90°"):::st
        MWAIT("16 空拍"):::st
        OWAIT("17 空拍"):::st
        DELAY3("27 旋转60°"):::st
        DELAY4("28 空拍"):::st
    end

    subgraph M8["模块97 节拍3"]
        ROT30B("18 旋转30°"):::st
        OWAITB("19 空拍"):::st
        MWAITB("20 空拍"):::st
        RWAITB("24 空拍"):::st
        WAIT1("29 空拍"):::st
        WAIT2("30 旋转30°"):::st
    end

    subgraph M9["模块98 整备C"]
        LK8("21 锁定"):::st
        SA8("22 转世"):::str
    end

    FREE1 --> LKA
    LKA -->|load| WD
    LKA -->|load| GK
    LKA -->|load| JOV
    LKA -->|load| OV
    LKA -->|load| JS
    SA -->|load| ZS
    WD -->|节拍1| ROT60
    GK -->|节拍1| ROT30
    ROT30 -->|节拍2| ROT60B
    JOV -->|节拍1| RWAIT
    RWAIT -->|节拍2| ROT90
    ROT30B --> LK8
    ROT90 --> OWAITB
    OWAITB --> LK8
    ROT60 --> MWAIT
    ROT60B --> MWAITB
    MWAIT -->|节拍3| ROT30B
    ROT28B --> OWAIT
    OWAIT --> RWAITB
    RWAITB --> SA8
    JS --> DELAY1
    DELAY1 --> DELAY3
    DELAY3 --> WAIT1
    WAIT1 --> LK8
    ZS --> ROT32B
    ROT32B --> DELAY4
    DELAY4 --> WAIT2
    WAIT2 --> LK8
    MWAITB --> LK8
    OV -->|节拍1| ROT28B
```

## load 配对判定

动作以**供方→接收方**成对产生（接收方 = 供方右侧相邻轮，编号减一），要么成对、要么整对不产生：

- 成对条件：接收方处于**锁定**、接收方为空（count=0）、接收方的右侧无料（可解锁）。
- 锁定 + count=1 → **美妙 + 接美妙**；锁定 + count≥2 → **超载 + 接超载**；转世 → **新生 + 接新生**。
- 不成对则双方保持原状态（供方保持锁定/转世），本拍均不动。
- 1号轮的右侧为出料口（虚拟接收位，永远为空、可解锁）：1号轮永远可以配对出料；它自身有料时不能作为接收方。

## 编号对照表

| 编号 | 卡片 | 所属模块 |
|---|---|---|
| 1 | 上电 | 模块90 上电初始化 |
| 2 | 自由 | 模块90 上电初始化 |
| 3 | 锁定 | 模块91 整备A |
| 4 | 转世 | 模块91 整备A |
| 5 | 美妙 | 模块92 美妙配对 |
| 6 | 接美妙 | 模块92 美妙配对 |
| 7 | 超载 | 模块93 超载配对 |
| 8 | 接超载 | 模块93 超载配对 |
| 9 | 新生 | 模块94 新生配对 |
| 10 | 接新生 | 模块94 新生配对 |
| 11 | 旋转60° | 模块95 节拍1 |
| 12 | 旋转30° | 模块95 节拍1 |
| 13 | 旋转28° | 模块95 节拍1 |
| 14 | 旋转60° | 模块96 节拍2 |
| 15 | 旋转90° | 模块96 节拍2 |
| 16 | 空拍 | 模块96 节拍2 |
| 17 | 空拍 | 模块96 节拍2 |
| 18 | 旋转30° | 模块97 节拍3 |
| 19 | 空拍 | 模块97 节拍3 |
| 20 | 空拍 | 模块97 节拍3 |
| 21 | 锁定 | 模块98 整备C |
| 22 | 转世 | 模块98 整备C |
| 23 | 空拍 | 模块95 节拍1 |
| 24 | 空拍 | 模块97 节拍3 |
| 25 | 旋转30° | 模块95 节拍1 |
| 26 | 旋转32° | 模块95 节拍1 |
| 27 | 旋转60° | 模块96 节拍2 |
| 28 | 空拍 | 模块96 节拍2 |
| 29 | 空拍 | 模块97 节拍3 |
| 30 | 旋转30° | 模块97 节拍3 |

## 模块90 上电初始化

状态：**自由**（唯一状态，上电后全部托架进入）。

```mermaid
flowchart TD
    classDef st fill:#000000,stroke:#ffffff,stroke-width:2px,color:#ffffff

    PWR([1 上电]):::st --> FREE1("2 自由"):::st
```

## 模块91 整备A

状态：**锁定、转世**。

```mermaid
flowchart TD
    classDef st fill:#000000,stroke:#ffffff,stroke-width:2px,color:#ffffff

    FREE1("2 模块90 自由"):::st --> LKA("3 锁定"):::st
    SA("4 转世"):::str
```

## 模块92 美妙配对

状态：**美妙、接美妙**。

```mermaid
flowchart TD
    classDef st fill:#000000,stroke:#ffffff,stroke-width:2px,color:#ffffff
    classDef str fill:#8b0000,stroke:#ffffff,stroke-width:2px,color:#ffffff
    classDef stg fill:#1b5e20,stroke:#ffffff,stroke-width:2px,color:#ffffff

    LKA("3 模块91 锁定"):::st -->|load| WD("5 美妙"):::st
    LKA -->|load| GK("6 接美妙"):::st
```

跨模块转移：模块91 的 **锁定** →（load）→ 模块92 美妙配对 的 **美妙 / 接美妙**。

## 模块93 超载配对

状态：**超载、接超载**。

```mermaid
flowchart TD
    classDef st fill:#000000,stroke:#ffffff,stroke-width:2px,color:#ffffff

    LKA("3 模块91 锁定"):::st -->|load| OV("7 超载"):::st
    LKA -->|load| JOV("8 接超载"):::st
```

跨模块转移：模块91 的 **锁定** →（load）→ 模块93 超载配对 的 **超载 / 接超载**。

## 模块94 新生配对

状态：**新生、接新生**。

```mermaid
flowchart TD
    classDef st fill:#000000,stroke:#ffffff,stroke-width:2px,color:#ffffff
    classDef str fill:#8b0000,stroke:#ffffff,stroke-width:2px,color:#ffffff

    SA("4 模块91 转世"):::str -->|load| ZS("9 新生"):::str
    LKA("3 模块91 锁定"):::st -->|load| JS("10 接新生"):::st
```

跨模块转移：模块91 的 **锁定** →（load）→ 模块94 新生配对 的 **接新生**；模块91 的 **转世** →（load）→ 模块94 新生配对 的 **新生**。

## 模块95 节拍1

元素：**旋转60°（11）、旋转30°（12、25）、旋转28°、旋转32°、空拍（23）**。

```mermaid
flowchart TD
    classDef st fill:#000000,stroke:#ffffff,stroke-width:2px,color:#ffffff
    classDef str fill:#8b0000,stroke:#ffffff,stroke-width:2px,color:#ffffff

    WD("5 模块92 美妙"):::st -->|节拍1| ROT60("11 旋转60°"):::st
    GK("6 模块92 接美妙"):::st -->|节拍1| ROT30("12 旋转30°"):::st
    OV("7 模块93 超载"):::st -->|节拍1| ROT28B("13 旋转28°"):::st
    JOV("8 模块93 接超载"):::st -->|节拍1| RWAIT("23 空拍"):::st
    JS("10 模块94 接新生"):::st --> DELAY1("25 旋转30°"):::st
    ZS("9 模块94 新生"):::str --> ROT32B("26 旋转32°"):::st
```

## 模块96 节拍2

元素：**旋转60°（14、27）、旋转90°（15）、空拍（16、17、28）**。

```mermaid
flowchart TD
    classDef st fill:#000000,stroke:#ffffff,stroke-width:2px,color:#ffffff
    classDef str fill:#8b0000,stroke:#ffffff,stroke-width:2px,color:#ffffff
    classDef stg fill:#1b5e20,stroke:#ffffff,stroke-width:2px,color:#ffffff

    ROT30("12 模块95 旋转30°"):::stg -->|节拍2| ROT60B("14 旋转60°"):::st
    RWAIT("23 模块95 空拍"):::st -->|节拍2| ROT90("15 旋转90°"):::st
    ROT60("11 模块95 旋转60°"):::st -->|节拍2| MWAIT("16 空拍"):::st
    ROT28B("13 模块95 旋转28°"):::st -->|节拍2| OWAIT("17 空拍"):::st
    DELAY1("25 模块95 旋转30°"):::st --> DELAY3("27 旋转60°"):::st
    ROT32B("26 模块95 旋转32°"):::st --> DELAY4("28 空拍"):::st
```

## 模块97 节拍3

元素：**旋转30°（18、30）、空拍（19、20、24、29）**。

```mermaid
flowchart TD
    classDef st fill:#000000,stroke:#ffffff,stroke-width:2px,color:#ffffff
    classDef str fill:#8b0000,stroke:#ffffff,stroke-width:2px,color:#ffffff
    classDef stg fill:#1b5e20,stroke:#ffffff,stroke-width:2px,color:#ffffff

    MWAIT("16 模块96 空拍"):::stg -->|节拍3| ROT30B("18 旋转30°"):::st
    ROT90("15 模块96 旋转90°"):::st -->|节拍3| OWAITB("19 空拍"):::st
    OWAIT("17 模块96 空拍"):::st -->|节拍3| RWAITB("24 空拍"):::st
    ROT60B("14 模块96 旋转60°"):::st --> MWAITB("20 空拍"):::st
    DELAY3("27 模块96 旋转60°"):::st --> WAIT1("29 空拍"):::st
    DELAY4("28 模块96 空拍"):::st --> WAIT2("30 旋转30°"):::st
```

## 模块98 整备C

状态：**锁定、转世**。

```mermaid
flowchart TD
    classDef st fill:#000000,stroke:#ffffff,stroke-width:2px,color:#ffffff
    classDef str fill:#8b0000,stroke:#ffffff,stroke-width:2px,color:#ffffff
    classDef stg fill:#1b5e20,stroke:#ffffff,stroke-width:2px,color:#ffffff

    ROT30B("18 模块97 旋转30°"):::stg --> LK8("21 锁定"):::st
    OWAITB("19 模块97 空拍"):::st --> LK8
    RWAITB("24 模块97 空拍"):::st --> SA8("22 转世"):::str
    MWAITB("20 模块97 空拍"):::stg --> LK8
    WAIT1("29 模块97 空拍"):::st --> LK8
    WAIT2("30 模块97 旋转30°"):::st --> LK8
```

跨模块转移：
- **美妙**：节拍1（旋转60°）→ 节拍2（空拍16）→ 节拍3（旋转30°）→ 整备C **锁定**。
- **接美妙**：节拍1（旋转30°）→ 节拍2（旋转60°）→ 节拍3（空拍20）→ 整备C **锁定**。
- **超载**：节拍1（旋转28°）→ 节拍2（空拍17）→ 节拍3（空拍24）→ 整备C **转世**。
- **新生**：模块94 新生配对 → 节拍1（旋转32°[26]）→ 节拍2（空拍28）→ 节拍3（旋转30°[30]）→ 整备C **锁定**。
- **接新生**：模块94 新生配对 接新生 → 节拍1（旋转30°[25]）→ 节拍2（旋转60°[27]）→ 节拍3（空拍29）→ 整备C **锁定**。
